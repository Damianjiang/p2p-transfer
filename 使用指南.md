# P2P传输程序使用指南

## 编译程序

```bash
cd p2p_transfer
make
```

## 基本使用

### 1. 主机模式（发布文件）

在一台电脑上运行主机：
```bash
./p2p_transfer -l 8888 -P 密码123
```

参数说明：
- `-l 8888` - 在端口8888上监听连接
- `-P 密码123` - 设置连接密码

### 2. 客户端模式（连接）

在另一台电脑上运行客户端：
```bash
./p2p_transfer -c 192.168.1.100 -p 8888 -P 密码123
```

参数说明：
- `-c 192.168.1.100` - 主机IP地址
- `-p 8888` - 主机端口
- `-P 密码123` - 房间密码

### 3. 连接后的命令

```
send 文件路径      - 发送文件
msg 消息内容       - 发送文本消息
info              - 显示连接信息
discover          - 发现对等方信息
help              - 显示帮助
quit              - 退出程序
```

## 实际使用场景

### 场景1：局域网传输（最简单）

**电脑A（发送方）：**
```bash
./p2p_transfer -l 8888 -P mysecret
```

**电脑B（接收方）：**
```bash
./p2p_transfer -c 192.168.1.100 -p 8888 -P mysecret
```

然后在电脑B上输入：
```
send /path/to/file.zip
```

### 场景2：互联网传输（需要端口转发）

**主机（发送方，在路由器后面）：**
1. 登录路由器管理界面
2. 配置端口转发：将外部端口8888转发到电脑的8888端口
3. 查询公网IP（例如：1.2.3.4）
4. 运行程序：
```bash
./p2p_transfer -l 8888 -P mysecret
```

**客户端（接收方）：**
```bash
./p2p_transfer -c 1.2.3.4 -p 8888 -P mysecret
```

### 场景3：通过VPN传输（推荐，最可靠）

**两台电脑都安装相同的VPN**（例如WireGuard、OpenVPN、Tailscale等）

**获取VPN分配的内网IP：**
- 电脑A的VPN IP：10.0.0.1
- 电脑B的VPN IP：10.0.0.2

**电脑A（发送方）：**
```bash
./p2p_transfer -l 8888 -P mysecret
```

**电脑B（接收方）：**
```bash
./p2p_transfer -c 10.0.0.1 -p 8888 -P mysecret
```

## 高级功能

### 使用用户名
```bash
./p2p_transfer -l 8888 -P secret -u Alice
```

### 查看程序版本
```bash
./p2p_transfer --help
```

## 注意事项

1. **防火墙设置**：确保两端防火墙允许相应端口的入站和出站连接
2. **NAT类型**：
   - 锥型NAT：最易连接
   - 对称型NAT：最困难，需要端口转发或VPN
3. **密码安全**：密码以明文传输，仅用于房间验证
4. **文件大小**：程序支持任意大小文件，带进度显示

## 常见问题

**Q: 连接被拒绝**
- 检查防火墙设置
- 确认端口号正确
- 确保主机程序正在运行

**Q: 连接超时**
- 检查网络连接
- 验证NAT配置
- 尝试使用VPN

**Q: 文件传输失败**
- 检查磁盘空间
- 验证文件权限
- 尝试小文件测试

## 使用Tailscale实现P2P（最简单方案）

1. 在两台电脑上安装Tailscale
2. 注册并登录
3. 连接两台设备到同一Tailscale网络
4. 获取Tailscale分配的IP（例如100.64.1.1和100.64.1.2）
5. 直接使用这些IP进行P2P连接，无需端口转发
